<% provide(:title, "Project Rating") %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
 
       <h1>
         <%= gravatar_for @current_student %>
         <%= @current_student.fname %> <%= @current_student.lname %>
         RATINGS
      </h1>
    </section>
  </aside>
</div>

<!-- creating and populating members array -->
<% @members = Array.new%>
<% @student.each do |student| %>
  <%  if student.team_id === current_student.team_id %>
    <% @members.push(student)%>
  <% end %>
<% end %>

<body>

  <div class="member_container">
    <h1 class="project_title">
      Team Members
    </h1>
    <!-- for each member in the current user's team -->
    <% @members.each do |member| %>
    <!-- member card -->
    <div class="member_box">
      <!-- member name -->
      <h2 class="member_name"> <%= member.fname %> <%= member.lname %></h2>
      <label class="rating_text" >Rating (out of 10)</label>
      
        <textarea class="text" id="textbox" ></textarea>
      <!-- comment box -->
      <textarea class="text" id="textbox" >Comments about this group member</textarea>
      <!-- submit button currenly hidden due to text dimensions-->
      <%= link_to "Submit", student_rating_path, class: "submitBtn" %>
    </div>
    <% end %>
  
  </div>
  
</body>
